{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "tooltip"],
  "permissions": [
    "core:default",
    "opener:default",
    "sql:default",
    "sql:allow-execute",
    "sql:allow-select",
    "dialog:default",
    "fs:allow-copy-file",
    "fs:allow-mkdir",
    "fs:allow-exists",
    "clipboard-manager:allow-read-text",
    "core:event:allow-emit",
    "core:event:allow-listen",
    {
      "identifier": "fs:scope",
      "allow": ["$APPDATA/covers", "$APPDATA/covers/**", "$APPDATA/profile", "$APPDATA/profile/**"]
    }
  ]
}
